<Page xmlns="http://schemas.nativescript.org/tns.xsd" navigatingTo="onNavigatingTo">
    <ActionBar title="My App" icon="" />

<ScrollView orientation="vertical">
    <StackLayout class="p-20">
        <!-- Hello world code -->
        <!-- <Label text="Tap the button" class="h1 text-center" />
        <Button text="TAP" tap="{{ onTap }}" class="-primary" />
        <Label text="{{ message }}" class="h2 text-center" textWrap="true" /> -->

        <TextView hint="{{ xHint }}" text="{{ xValue }}"></TextView>
        <Button text="Submit" tap="{{ xOnSubmitButton }}" class="-primary" />
        <Button text="Clear" tap="{{ xOnClearButton }}" class="-primary" />
        <TextView hint="Output" text="{{ xResult }}"></TextView>
    </StackLayout>
</ScrollView>
</Page>
